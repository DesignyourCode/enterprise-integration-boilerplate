jQuery.noConflict();

(function($) {

    // It is likely your client is already using jQuery in their site.
    // To avoid unnecessary conflicts, just add your code here.
    // This file has already been set up to avoid conflicts.

})(jQuery);